// jsに変換して実行
// $ npm run build
// node dist/index.js
// tsを直接実行
// npx ts-node src/index.ts
var hello = "hello";
console.log(hello);
// function
function add(a, b) {
    return a + b;
}
// obgect
var person = {
    name: 'Jack',
    age: 27
};
// array
var fruits = ['Apple', 'Banana', 'Grape'];
// tuple型
var book = ['boolean', 1500, false];
console.log(person);
